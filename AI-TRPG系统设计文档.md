# AI TRPG 系统设计文档

> 目标：3-4 名玩家 + AI 担任 DM 的网页版跑团系统
> 阶段：设计阶段（规则 + 流程 + 架构）

---

## 1. 核心设计理念

AI 当 DM 最大的挑战不是"会不会讲故事"，而是三件事：

1. **规则执行的稳定性**——AI 容易忘记规则、算错数值、骰子结果不公平（自己编）
2. **多人节奏管理**——3-4 人同时想发言，容易乱序、抢戏、被冷落
3. **长程记忆**——一场跑团可能几十上百轮对话，AI 会忘记之前发生的事

所以整个系统设计的核心思路是：**AI 只负责"叙事"，规则判定和随机数交给确定性的代码逻辑**，两者通过结构化输出对接。这样既保留 AI 的创造力，又保证游戏的公平性和一致性。

---

## 2. 规则系统建议：轻量自创规则（PbtA 风格）

不建议直接照搬 DND 5e 这类规则复杂的系统——太多表格、职业技能、战斗回合细则，AI 很容易算错或遗漏，玩家体验会很割裂。

推荐参考 **PbtA（Powered by the Apocalypse）/ FATE** 的轻量化思路，理由：

- 判定机制简单，AI 和代码都容易稳定处理
- 结果分级（大成功/成功但有代价/失败）天然适合"叙事化"包装，AI 发挥空间大
- 属性少，角色卡轻量，创角快

### 2.1 核心判定机制

```
掷 2d6 + 属性修正值
  10 分及以上 → 完全成功
  7-9 分     → 部分成功（成功但有代价/附加条件）
  6 分及以下 → 失败（剧情推进，通常伴随负面后果）
```

### 2.2 属性建议（6 项，-2 到 +3 区间）

| 属性 | 用途 |
|---|---|
| 力量 STR | 近战、强行突破 |
| 敏捷 DEX | 潜行、闪避、精细操作 |
| 感知 WIT | 观察、直觉、察觉陷阱 |
| 魅力 CHA | 社交、说服、威吓 |
| 意志 WIL | 抗压、心智抵抗、专注 |
| 知识 KNO | 知识判定、解谜、专业技能 |

### 2.3 资源与生命值

- **生命值（HP）**：简化为 3-5 点的"伤害轨道"（轻伤/重伤/濒死），而不是精确数值扣血，减少 AI 记账负担
- **压力值/状态效果**：用简短标签表示（如"中毒""恐惧""激励"），AI 只需在叙事中体现，具体数值交给系统追踪

### 2.4 成长机制

不用经验值累计，改用**里程碑成长**（完成关键剧情节点后获得成长点，用于提升属性或获得特殊能力）——降低 AI 计算负担，且更契合叙事节奏。

> 这套规则可以完全自定义调整，只是给你一个"AI 友好"的起点，核心原则是：**规则越简单确定，AI 执行越稳，玩家体验越流畅**。

---

## 3. 世界观模组系统（模组导入）

这是让系统真正可复用、可扩展的关键功能——不自己写死一套世界观，而是让创作者能把自己设计的模组（跑团模块/剧本）导入进来，AI 读取并运行。

### 3.1 模组的组成部分

一个完整模组应包含：

| 组成部分 | 说明 |
|---|---|
| 世界观基础设定 | 背景、基调、时代、特殊规则微调 |
| 地点/场景图谱 | 各场景的描述、彼此的连接关系、进入/触发条件 |
| NPC 名册 | 性格、目标、对话钩子、与其他 NPC/主线的关系 |
| 剧情线 | 主线节点、支线、关键决策点、可能的结局分支 |
| 遭遇/事件库 | 战斗遭遇、陷阱、谜题、随机事件表 |
| 物品/道具清单 | 特殊道具及其效果 |
| 规则覆盖（可选） | 若模组需要不同于系统默认的判定难度/特殊机制 |

### 3.2 模组格式：结构化 + 自由文本双轨支持

- **结构化格式（推荐官方模板）**：创作者按 JSON/YAML 模板填写，系统可直接解析，运行最稳定
- **自由文本导入**：创作者只有一份 Markdown/PDF 大纲或成熟的商业模组文本，系统在导入时用 AI 做**一次性 ETL（提取-转换-加载）**——解析文本，抽取出场景、NPC、剧情节点等结构化数据，生成预览，创作者确认/修正后正式入库

这样既保证专业创作者能精确控制，也降低普通用户的使用门槛。

### 3.3 模组运行机制：分块加载，而非整篇塞入上下文

模组可能有几十个场景、大量 NPC，不能整份丢进 AI 的 prompt（成本高、且信息过载会让 AI 表现变差）。运行时采用类似 **RAG（检索增强）** 的思路：

```
固定常驻层（始终在 context）：
  世界观基调摘要 + 当前剧情大纲位置 + 核心规则

动态检索层（按需注入）：
  当前场景完整描述
  当前场景内活跃的 NPC 详情
  与当前场景直接关联的地点/线索
  玩家已触发但尚未解决的支线钩子
```

模组在导入阶段就被拆解为一个个"场景节点"存入数据库，运行时后端根据当前游戏状态（玩家在哪、触发了什么）动态检索相关节点注入 prompt，而不是让 AI 记住整个模组。

### 3.4 模组兼容性

不同创作者写的模组，敌人强度、特殊机制可能各不相同。建议定义**统一的数值接口**（复用第 2 节的属性/难度体系），模组作者在填写 NPC/敌人数值时对照统一标准，确保所有模组都能在同一套判定引擎下正常运行。

### 3.5 模组导入流程（用户视角）

```
1. 上传模组文件（结构化模板 或 自由文本）
2. 若为自由文本 → AI 解析生成结构化预览 → 创作者确认/编辑
3. 系统校验完整性（是否缺少关键场景/开场钩子等）
4. 拆解为场景节点，存入模组库
5. 创建房间时可选择该模组作为本局世界观
```

---

## 4. 动态房间配置

不把"存档模式"和"战斗复杂度"写死在系统里，而是做成**创建房间时的动态选项**，由房主（或全员投票）在开局前选择。

### 4.1 可配置项

| 配置项 | 选项 | 说明 |
|---|---|---|
| 战役模式 | 单局模式 / 长期战役模式 | 单局：结束即清空，无需存档；长期：跨 session 保存进度，支持中途暂停继续 |
| 战斗复杂度 | 纯叙事战斗 / 战术战斗 | 纯叙事：无位置概念，靠判定+描述推进；战术：引入简化网格/位置，行动有距离和站位限制 |
| 判定难度基准 | 简单 / 标准 / 硬核 | 影响默认检定难度线，硬核模式失败后果更重 |
| 内容分级 | 常规 / 成人向 | 影响 AI 叙事尺度（涉及暴力/黑暗题材的呈现程度） |

### 4.2 数据结构示例

```json
{
  "room_id": "r001",
  "module_id": "m001",
  "campaign_mode": "persistent",
  "combat_mode": "tactical",
  "difficulty": "standard",
  "content_rating": "standard"
}
```

### 4.3 配置如何影响系统行为

这份 `room_config` 会被系统的多个模块读取：

- **AI 编排层**：根据 `campaign_mode` 决定是否加载历史存档摘要；根据 `combat_mode` 切换战斗相关的 prompt 模板（叙事化描述 vs 需要输出坐标/距离信息）
- **规则引擎**：根据 `combat_mode` 决定是否启用位置/网格判定逻辑；根据 `difficulty` 调整判定阈值
- **存档系统**：`campaign_mode` 为长期战役时，每个 session 结束自动生成剧情摘要存档；单局模式则不生成持久化记录

### 4.4 房间创建流程（更新版）

```
1. 房主创建房间
2. 选择世界观来源：导入模组 / 无模组自由发挥
3. 选择战役模式（单局 / 长期）
4. 选择战斗复杂度（叙事 / 战术）
5. （可选）判定难度、内容分级
6. 生成邀请码，其他玩家加入
7. 进入引导式创角流程
```

---

## 5. 系统整体架构

```
┌─────────────────────────────────────────┐
│              前端（Web App）              │
│  房间大厅/设置 / 角色卡 / 跑团界面 / 模组管理 │
└─────────────────┬─────────────────────────┘
                   │ WebSocket（实时）
┌─────────────────┴─────────────────────────┐
│               后端服务层                    │
│ ┌──────────┐┌──────────┐┌────────────┐    │
│ │ 会话管理  ││ 规则引擎  ││ 状态管理器  │    │
│ │(房间/轮次)││(判定/骰子)││(角色/世界)  │    │
│ └──────────┘└──────────┘└────────────┘    │
│ ┌──────────┐┌──────────────────────────┐  │
│ │ 模组检索  ││   AI 编排层（Prompt 组装） │  │
│ │(场景节点) ││ room_config + 当前场景     │  │
│ │           ││ + 角色卡摘要 + 记忆         │  │
│ └──────────┘└─────────────┬──────────────┘ │
└───────────────────────────┼─────────────────┘
                             │
                      ┌──────┴──────┐
                      │  Claude API │
                      └─────────────┘
```

关键设计原则：**AI 不直接掌控游戏状态，只读取状态、输出叙事+结构化指令，由后端确定性代码去更新真正的状态。** 这样可以避免 AI"记错血量"或"编造不存在的物品"这类一致性问题。

---

## 6. 核心模块设计

### 6.1 角色创建

- 创角流程建议做成**引导式问答**（AI 逐步提问："你的角色叫什么？""是什么出身？""擅长什么？"），自动生成角色卡草稿，玩家可微调
- 若已选择模组，创角环节可结合模组世界观给出背景选项（如模组指定的种族/阵营）

角色卡数据结构示例：

```json
{
  "character_id": "c001",
  "name": "薇拉",
  "background": "流浪剑客",
  "attributes": { "STR": 2, "DEX": 3, "WIT": 1, "CHA": 0, "WIL": 1, "KNO": -1 },
  "hp_track": ["轻伤", "轻伤", "重伤"],
  "inventory": ["旧剑", "干粮 x2"],
  "status_effects": [],
  "milestones": 0
}
```

### 6.2 游戏主循环（Game Loop）

```
1. AI 描述当前场景/情境（若有模组，从模组检索当前场景节点）
2. 玩家们各自输入行动（自由发言，非强制轮流）
3. 后端将行动排队 → AI 逐一解读意图
4. AI 判断该行动是否需要检定
      → 需要：AI 输出"请求检定"结构化指令（含属性、难度）
        → 前端/后端生成真实随机数（不是 AI 编的）
        → 结果回传给 AI
      → 不需要：AI 直接叙事推进
5. AI 输出叙事文本 + 状态更新指令（JSON）
6. 后端解析指令，更新角色卡/世界状态，广播给所有玩家
7. 回到 1，进入下一情境
```

**关键点：骰子必须是真随机数由代码生成，AI 只负责判断"是否需要骰"和"骰的难度"，绝不能让 AI 自己声称骰子结果**——否则玩家会觉得不公平，也容易让 AI 顺着剧情"作弊"。

### 6.3 多人协作机制（3-4 人自由发言）

- **意图队列**：玩家的行动进入短暂的"缓冲窗口"（如 15-30 秒），窗口内的所有行动一起提交给 AI，AI 按逻辑顺序合并处理
- **紧急打断**：允许玩家标记"紧急动作"（如战斗中的即时反应），优先处理
- **冷场保护**：若某玩家长时间未发言，AI 可主动给该角色分配一个"聚光灯时刻"，避免被忽略

### 6.4 AI DM 提示工程设计

System Prompt 分层组装，每轮请求动态拼接：

```
【固定层】DM 人格设定 + room_config（模式/难度/分级）+ 输出格式规范
【世界观层】模组世界观摘要（若有模组）或自由创作设定
【中期记忆层】当前章节剧情摘要（AI 生成的滚动摘要，每 N 轮更新一次）
【短期记忆层】最近 5-10 轮完整对话
【状态层】当前所有角色卡状态 + 世界状态 + 当前检索到的模组场景节点
【本轮输入】玩家们本轮的行动
```

### 6.5 输出格式规范（结构化输出）

```json
{
  "narrative": "薇拉的剑锋堪堪擦过对方肩膀……",
  "requires_roll": { "character": "c001", "attribute": "DEX", "difficulty": "normal" },
  "state_updates": [
    { "type": "hp_change", "character": "c002", "delta": -1 },
    { "type": "item_gain", "character": "c001", "item": "破损的地图" }
  ],
  "scene_flags": { "location": "废弃神殿", "tension": "high" }
}
```

---

## 7. 技术栈建议

| 层 | 建议方案 |
|---|---|
| 前端 | Next.js + React，WebSocket 做实时同步 |
| 后端 | Node.js（与前端同栈，方便共享类型）或 Python（规则引擎逻辑更清晰） |
| 实时通信 | WebSocket（房间广播、多人同步） |
| AI 调用 | Claude API，用 Tool Use / 结构化输出保证格式稳定 |
| 模组存储/检索 | PostgreSQL 存结构化数据 + 向量检索（用于自由文本模组的场景节点匹配） |
| 数据库 | PostgreSQL（角色卡、房间、历史记录、模组库）+ Redis（会话态、队列） |
| 骰子/随机数 | 后端生成，绝不交给 AI |

---

## 8. 开发路线图建议

**MVP（先跑通核心体验）**
1. 单房间、3-4 人加入、简单角色卡（模板创角）
2. 支持一种结构化模组格式导入（先不做自由文本 ETL）
3. AI DM 基础叙事 + 简单判定，手动触发骰子
4. 房间配置先只做"战役模式"和"战斗复杂度"两个开关

**V2**
5. 引导式创角、意图队列、结构化状态更新自动生效
6. 分层记忆系统（滚动摘要）+ 长期战役存档
7. 自由文本模组导入（AI 辅助 ETL + 预览确认）

**V3**
8. 里程碑成长系统、战术战斗模式（位置/网格）、模组间数值兼容性校验
9. 模组市场/分享机制（如果想让其他人也能上传分享模组）

---

## 9. 项目定位与暂缓事项

**使用范围**：私人自用，仅限本人与朋友几人内部跑团，不公开发布/分发。因此模组来源不受版权限制，可以自由导入已有商业/开源模组内容作为素材，模组格式设计可以更贴合实际拿到手的资料形式，不必考虑分享给陌生人时的规范化问题。

**暂缓事项**：战术战斗模式（位置/网格系统）推迟到后续有具体想法后再设计。当前 MVP 及后续开发先聚焦**纯叙事战斗**（`combat_mode: "narrative"`），`room_config` 中预留 `tactical` 选项但暂不实现对应逻辑。

**仍可进一步细化的问题**：
- 结构化模板具体字段设计（可以再单独细化一份"模组作者填写指南"，方便你自己整理已有的模组资料）
